# aurisaurea.github.io

This is the website of the choir auris aurea. It uses a minimalistic setup of the minimal mistakes jekyll theme.

## Local development

The site can be built locally using

```bundle exec jekyll serve```